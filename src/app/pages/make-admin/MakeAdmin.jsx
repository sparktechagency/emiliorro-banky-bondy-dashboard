import Title from "@/components/ui/Title";

const MakeAdmin = () => {
    return (
        <div>
            <Title title="Make Admin" />
            this is make admin
        </div>
    );
};

export default MakeAdmin;